# 에이전트 작업 지침

## 저장소의 목적

이 저장소는 Excel 매입·매출·재고 장부를 읽어 신뢰할 수 있는 분석 데이터로 변환하고, 이를 바탕으로 검증 결과와 HTML 보고서를 생성하는 도구이다.

전체 작업은 다음 세 계층으로 구분한다.

1. 데이터 처리 계층
2. 분석·계산 계층
3. 리포트 계층

에이전트는 변경하려는 코드가 어느 계층에 속하는지 먼저 판단하고, 다른 계층의 책임을 불필요하게 끌어오지 않는다.

```text
Excel 원본
  ↓
데이터 처리: 파싱·정규화·형식 검증
  ↓
정규화 데이터
  ↓
분석·계산: 대사·재고·원가·손익·집계
  ↓
분석 결과
  ↓
리포트: 표·차트·설명·HTML 렌더링
```

## 1. 데이터 처리 계층

### 목적

원본 Excel의 구조와 표현 차이를 제거하여, 분석 계층이 안정적으로 사용할 수 있는 정규화 데이터를 만든다.

### 주요 책임

- 매입·매출·재고 Excel 파일 읽기
- 시트와 헤더 행 식별
- 원본 열 이름을 표준 필드명으로 매핑
- 날짜, 수량, 단가, 금액, 제품 ID의 자료형 통일
- 공백, 쉼표, 빈 셀, 소계·합계 행 처리
- 필수 필드 누락과 해석할 수 없는 값 검출
- 원본 파일명, 시트명, Excel 행 번호 등 추적 정보 보존
- 정규화된 JSON 생성

### 경계

- 원가, 이익, 재고평가액과 같은 회계·업무 지표를 계산하지 않는다.
- 차트 제목, 색상, HTML 구조와 같은 표시 규칙을 포함하지 않는다.
- 원본 값이 불명확할 때 임의의 업무 가정을 적용하지 않는다. 보정이 필요하면 보정 사실과 원본 값을 추적할 수 있어야 한다.

### 관련 파일

- `parse_workbooks.py`
- `parse_sales_voucher_metadata.py`

## 2. 분석·계산 계층

### 목적

정규화 데이터를 대사하고 검증하여, 보고서에서 직접 사용할 수 있는 업무적·회계적 분석 결과를 만든다.

### 주요 책임

- 매입·매출·재고 데이터 간 제품 ID 대사
- 품명·규격·단가 불일치 검출
- 매입 없이 발생한 매출, 음수 재고, 수량 차이 검출
- 품목·거래처·기간별 집계
- 재고 수량과 재고 금액 계산
- 매출원가, 매출총이익, 이익률 계산
- 주간·월간 등 기간별 분석 데이터 생성
- 분석 결과와 검증 결과를 JSON 또는 다른 구조화된 형식으로 제공

### 경계

- HTML 태그, CSS, Plotly 레이아웃과 같은 표현 세부사항을 포함하지 않는다.
- 금액을 천 단위 문자열로 바꾸는 등 화면 표시용 서식을 적용하지 않는다.
- 계산식과 업무 가정은 코드와 결과에서 확인할 수 있어야 한다.
- 같은 입력에는 같은 결과를 내는 결정적 계산을 우선한다.

### 회계·업무 원칙

- 매출액을 이익으로 간주하지 않는다.
- 매출원가와 재고금액은 적용한 원가 기준을 명시한다. 예: 평균원가, 최종매입가, 선입선출법.
- 기초재고를 반영하지 않은 계산은 그 사실을 명시한다.
- `기초재고 + 매입 - 출고 = 기말재고` 관계에서 어느 수량과 금액을 사용했는지 분명히 한다.
- 부가세 포함 금액과 공급가액을 혼용하지 않는다.
- 대사 차이, 음수 재고, 누락 ID를 임의로 상계하거나 숨기지 않는다.
- 근거가 부족한 금액은 확정 이익이 아니라 추정치 또는 대사 잔액으로 표시한다.

### 관련 파일

- `analyze_validation.py`
- `analyze_inventory.py`
- 분석용 집계와 계산 함수

현재 일부 분석 함수가 리포트 생성 코드 안에 있을 수 있다. 기존 동작을 수정할 때에는 계산과 표현을 구분하고, 재사용 가치가 있는 계산은 분석 계층으로 분리하는 방향을 우선한다.

## 3. 리포트 계층

### 목적

분석 계층의 결과를 사람이 검토하고 의사결정할 수 있도록 표, 차트, 설명과 HTML 문서로 표현한다.

### 주요 책임

- 핵심 요약 지표 배치
- 검증 결과와 상세 표 구성
- Plotly 또는 HTML/SVG 차트 생성
- 보고서 제목, 단위, 축 이름, 범례, 설명 문구 적용
- A4 출력과 화면 열람을 고려한 HTML 레이아웃 구성
- 보고서 설정 파일 반영
- 공개용 미리보기 이미지 생성

### 경계

- 원본 Excel을 직접 읽지 않는다.
- 파싱 오류를 화면 코드에서 보정하지 않는다.
- 보고서에 필요한 핵심 수치가 없으면 리포트 계층에서 새 계산식을 숨겨 추가하지 말고 분석 계층의 산출물을 확장한다.
- 표시 단위 변환, 반올림, 정렬, 상위 N개 선택처럼 표현을 위한 가벼운 변환만 수행한다.
- 차트 모양과 문구를 바꾸는 작업은 계산 결과를 변경하지 않아야 한다.

### 관련 파일

- `generate_analysis_report_html.py`
- `generate_analysis_report_html_core.py`
- `src/charts.py`
- `report_spec.yaml`
- `capture_html_a4.py`

## 계층 간 인터페이스 원칙

- 데이터 처리 계층은 정규화된 필드명과 자료형을 안정적으로 제공한다.
- 날짜는 가능한 한 ISO 형식인 `YYYY-MM-DD`를 사용한다.
- 수량과 금액은 분석이 끝나기 전까지 문자열이 아닌 숫자로 유지한다.
- 분석 결과에는 계산 기준, 단위, 적용 기간을 알 수 있는 메타데이터를 포함한다.
- 리포트는 분석 결과의 필드 의미를 다시 추론하지 않고 명시된 계약에 따라 사용한다.
- 한 변경이 여러 계층에 걸치면 각 계층의 입력·출력 변경을 함께 설명하고 계층별 테스트를 추가한다.

## 변경 위치 판단 예시

- Excel 열 이름을 인식하지 못한다: 데이터 처리 계층에서 수정한다.
- 재고 수량이나 이익 계산이 틀렸다: 분석·계산 계층에서 수정한다.
- 표의 열 순서, 차트 색상, 폰트 크기가 부적절하다: 리포트 계층에서 수정한다.
- 보고서에서 누락 데이터를 임의로 0으로 바꾸지 않는다. 필요한 처리 규칙은 데이터 처리 또는 분석 계층에 명시한다.

## 공개 저장소와 민감 데이터

이 저장소는 공개 가능한 상태를 유지해야 한다.

- 실제 고객사·공급사 장부를 커밋하지 않는다.
- 실제 장부에서 변환한 JSON, 검증 Excel, 전표별 파일, 실제 HTML 보고서를 커밋하지 않는다.
- 비공개 파일은 `private_sources/`, `private_intermediate/`, `private_reports/`에 둔다. 이 디렉터리는 Git에서 제외되어야 한다.
- 공개 테스트, 스크린샷, README 예시는 `examples/dummy_json/`의 더미 데이터를 사용한다.
- 생성된 HTML 보고서는 버전 관리에 포함하지 않는다.
- 공개 시각 미리보기는 `docs/assets/report-preview.png`를 사용한다.
- 실제 거래처명, 품목명, 금액, 파일 경로가 테스트 코드·로그·문서에 남지 않았는지 확인한다.

## 더미 보고서 및 공개 미리보기 갱신

- 코드 변경 후에는 더미 데이터를 사용하여 HTML 보고서가 정상 생성되는지 검증한다.
- 생성된 더미 HTML 보고서는 검증용 산출물이며 Git에 커밋하지 않는다.
- 보고서의 레이아웃, 표, 차트, 문구, 표시 지표 또는 스타일이 변경되어 공개 미리보기의 화면이 달라지는 경우 `docs/assets/report-preview.png`를 다시 생성하여 함께 갱신한다.
- 데이터 처리, 내부 계산 또는 테스트만 변경되어 공개 보고서 화면에 변화가 없는 경우 미리보기 PNG를 갱신하지 않아도 된다.

## 구현 원칙

- 로컬 절대 경로를 하드코딩하지 말고 명령행 옵션과 설정 파일을 우선한다.
- 보고서 제목, 차트 제목, 축 이름, 표시 단위, 색상처럼 조정 가능한 표현 값은 가능한 한 `report_spec.yaml`에서 관리한다.
- 계산 함수와 차트 함수는 가능한 한 입력과 출력이 분명한 작은 단위로 유지한다.
- 오류를 조용히 무시하지 말고 원인과 대상 행을 확인할 수 있는 메시지를 제공한다.
- 기존 결과의 의미가 바뀌는 경우 필드명, 문서, 테스트를 함께 갱신한다.
- 범위를 벗어난 대규모 정리는 별도 작업으로 분리한다.

## 검증 원칙

변경한 계층에 맞는 검증을 수행한다.

- 데이터 처리 변경: 헤더 변형, 빈 값, 숫자·날짜 변환, 소계·합계 행 제외를 검증한다.
- 분석 변경: 정상 사례, 경계값, 음수 재고, 누락 ID, 수량 불일치, 원가 기준을 검증한다.
- 리포트 변경: 필수 섹션, 표와 차트 데이터 연결, 설정 반영, HTML 생성 여부를 검증한다.
- 회계 지표 변경: 작은 예제 데이터로 계산식을 손으로 재현할 수 있어야 한다.

커밋 전 전체 테스트를 실행한다.

```bash
.venv/bin/python -m unittest discover -s . -p 'test_*.py'
```

테스트를 실행할 수 없는 환경에서는 실행하지 못한 이유와 확인하지 못한 범위를 명확히 남긴다.